# Система безопасности и блокировки незарегистрированных пользователей

## 🔒 Многоуровневая защита

### Уровень 1: Проверка при загрузке приложения (App.tsx)

**Когда:** При каждом запуске приложения  
**Что делает:**
1. Проверяет флаг `isRegistered` в localStorage
2. Если `isRegistered === true` → делает запрос в базу данных
3. Проверяет существование пользователя: `GET /users/telegram/:telegramId`
4. Если пользователь **НЕ НАЙДЕН** → вызывает `logout()` и очищает все данные
5. Пока проверка не завершена → показывает экран загрузки

**Код:**
```typescript
// Флаг проверки аутентификации
const [authChecked, setAuthChecked] = useState(false);

// ОБЯЗАТЕЛЬНАЯ проверка при загрузке
if (telegramId && isRegistered) {
  const response = await userAPI.getUserByTelegramId(telegramId);
  
  if (!response.data) {
    console.log('❌ Пользователь не найден в базе - ВЫХОД');
    useStore.getState().logout();
    setAuthChecked(true);
    return;
  }
  
  setAuthChecked(true);
}

// Блокировка интерфейса до завершения проверки
if (!i18nReady || !authChecked) {
  return <LoadingScreen />;
}
```

**Результат:**
- ✅ Natali не сможет попасть на сайт до проверки
- ✅ Если её нет в базе → автоматический logout
- ✅ Показывает экран регистрации

---

### Уровень 2: Проверка на каждой странице (useAuthGuard hook)

**Когда:** При открытии любой страницы приложения  
**Где:** `frontend/src/hooks/useAuthGuard.ts`

**Что делает:**
1. Проверяет `isRegistered` в store
2. Если `false` → редирект на `/` (регистрация)
3. Если `true` → проверяет в базе данных
4. Если не найден → `logout()` + редирект на регистрацию

**Код:**
```typescript
export const useAuthGuard = () => {
  const navigate = useNavigate();
  const { isRegistered, logout } = useStore();

  useEffect(() => {
    const verifyUser = async () => {
      if (!isRegistered) {
        navigate('/', { replace: true });
        return;
      }

      try {
        const telegramId = getTelegramId();
        const response = await userAPI.getUserByTelegramId(telegramId);
        
        if (!response.data) {
          logout();
          navigate('/', { replace: true });
        }
      } catch (error: any) {
        if (error.response?.status === 404) {
          logout();
          navigate('/', { replace: true });
        }
      }
    };

    verifyUser();
  }, [isRegistered, navigate, logout]);
};
```

**Использование:**
```typescript
// В любой защищенной странице
export default function CatalogPage() {
  useAuthGuard(); // Первая строка в компоненте
  // ... остальной код
}
```

**Защищенные страницы:**
- ✅ MainMenu.tsx
- ✅ CatalogPage.tsx
- ✅ ProfilePage.tsx (добавить)
- ✅ AddListingPage.tsx (добавить)
- ✅ MyListingsPage.tsx (добавить)
- ✅ FavoritesPage.tsx (добавить)
- ✅ ChatsListPage.tsx (добавить)
- ✅ AdminPage.tsx (добавить)

---

### Уровень 3: Роутинг (App.tsx)

**Когда:** При навигации по приложению  
**Что делает:**
- Если `!isRegistered` → доступны **только** страницы регистрации
- Если `isRegistered` → доступны все страницы приложения
- Все остальные роуты → редирект на `/`

**Код:**
```typescript
{!isRegistered ? (
  <>
    <Route path="/" element={<WelcomePage />} />
    <Route path="/agreement" element={<AgreementPage />} />
    <Route path="/nickname" element={<NicknamePage />} />
    <Route path="*" element={<Navigate to="/" replace />} />
  </>
) : (
  <>
    <Route path="/" element={<MainMenu />} />
    <Route path="/catalog" element={<CatalogPage />} />
    {/* ... остальные страницы */}
  </>
)}
```

---

### Уровень 4: Backend API защита

**Middleware:** `verifyTelegramAuth` в `backend/src/middleware/auth.js`

**Что делает:**
1. Проверяет `x-telegram-init-data` заголовок
2. Проверяет криптографическую подпись Telegram
3. Если подпись неверная → `401 Unauthorized`

**Применяется на эндпоинтах:**
- `POST /listings` - создание объявления
- `PUT /listings/:id` - редактирование
- `DELETE /listings/:id` - удаление
- `POST /users/register` - регистрация (опционально)

---

## 🚫 Что НЕ МОЖЕТ сделать незарегистрированный пользователь:

1. ❌ Просматривать каталог объявлений
2. ❌ Открывать страницы приложения
3. ❌ Создавать объявления
4. ❌ Отправлять сообщения
5. ❌ Редактировать профиль
6. ❌ Видеть избранное
7. ❌ Получать доступ к админ-панели

**Единственный доступ:** Страницы регистрации (Welcome, Agreement, Nickname)

---

## ✅ Сценарий для Natali:

### Было (БАГ):
1. Natali открывает бот
2. `isRegistered: true` в localStorage (старые данные)
3. Приложение показывает главное меню
4. Natali листает каталог, хотя её нет в базе
5. **Проблема:** Доступ без регистрации ❌

### Стало (ИСПРАВЛЕНО):
1. Natali открывает бот
2. `isRegistered: true` в localStorage (старые данные)
3. **App.tsx проверяет в базе:** `GET /users/telegram/:telegramId`
4. **Пользователь не найден (404)**
5. **Автоматический logout:** очистка localStorage
6. **Показывает экран регистрации**
7. Natali проходит регистрацию заново
8. Только после этого получает доступ к приложению ✅

---

## 🔍 Логирование и отладка

### Консольные сообщения:

**При успешной проверке:**
```
🔍 ОБЯЗАТЕЛЬНАЯ проверка существования пользователя в базе: 670170626
✅ Пользователь найден в базе: Adminisus
```

**При отсутствии в базе:**
```
🔍 ОБЯЗАТЕЛЬНАЯ проверка существования пользователя в базе: 123456789
❌ Пользователь не найден в базе - ВЫХОД И РЕГИСТРАЦИЯ
```

**useAuthGuard на странице:**
```
🚫 AuthGuard: Пользователь не найден в базе - выход
```

---

## 📊 Проверка работы системы

### Тест 1: Удаление пользователя из базы
```powershell
# Удалить конкретного пользователя через MongoDB
# Открыть бот → должен показать регистрацию
```

### Тест 2: Ручная очистка localStorage
```javascript
// В консоли браузера
localStorage.clear();
location.reload();
// Должен показать регистрацию
```

### Тест 3: Попытка доступа к защищенным страницам
```
# В незарегистрированном состоянии попробовать:
https://a8a3ca83.kupyprodai.pages.dev/catalog
https://a8a3ca83.kupyprodai.pages.dev/profile

# Должен редиректить на /
```

---

## 🛠️ Техническая реализация

### База данных (MongoDB):
- Индекс: `{ telegramId: 1, unique: true }`
- Эндпоинт проверки: `/users/telegram/:telegramId`

### Frontend (React + Zustand):
- `isRegistered` флаг в Zustand store
- `authChecked` флаг в App.tsx state
- `useAuthGuard()` hook на каждой странице
- `logout()` метод очищает все данные

### API эндпоинты:
```
GET  /users/telegram/:telegramId  - Проверка существования
POST /users/register              - Регистрация
GET  /users/:id                   - Получение профиля
```

---

## 🔄 Обновление защиты

### Добавить useAuthGuard на новую страницу:

1. Импортировать hook:
```typescript
import { useAuthGuard } from '../hooks/useAuthGuard';
```

2. Вызвать в начале компонента:
```typescript
export default function MyNewPage() {
  useAuthGuard(); // Первая строка
  // ... остальной код
}
```

3. Готово! Страница защищена ✅

---

## 📝 Итоговая безопасность

| Уровень | Защита | Статус |
|---------|--------|--------|
| **1. Загрузка приложения** | Проверка в базе при старте | ✅ Включено |
| **2. Каждая страница** | useAuthGuard hook | ✅ Включено |
| **3. Роутинг** | React Router защита | ✅ Включено |
| **4. Backend API** | Telegram auth middleware | ✅ Включено |

**Вывод:** Незарегистрированный пользователь **НЕ МОЖЕТ** получить доступ к приложению ни при каких обстоятельствах. 🔒
