# 🧹 Очистка базы данных от дубликатов

## Проблема
Создались дубликаты пользователей с одинаковыми никнеймами но разными ID из-за бага в getTelegramId().

## Решение

### Вариант 1: Полная очистка (рекомендуется для теста)

Зайдите в MongoDB Atlas:
1. Откройте коллекцию `users`
2. Удалите ВСЕ документы
3. Откройте коллекцию `chats`
4. Удалите ВСЕ документы
5. Откройте коллекцию `listings`
6. Опционально: можете оставить объявления или тоже удалить

### Вариант 2: Удалить только тестовые аккаунты

Удалите пользователей с ID начинающимся на:
- `999999999` (старый фиксированный тестовый ID)
- `temp_` (временные ID)
- `local_` (старые локальные ID)

```javascript
// В MongoDB Atlas Shell
db.users.deleteMany({ 
  $or: [
    { id: /^999999999/ },
    { id: /^temp_/ },
    { id: /^local_/ },
    { telegramId: /^999999999/ },
    { telegramId: /^temp_/ },
    { telegramId: /^local_/ }
  ]
})

// Удалить связанные чаты
db.chats.deleteMany({
  $or: [
    { participant1: /^999999999/ },
    { participant1: /^temp_/ },
    { participant1: /^local_/ },
    { participant2: /^999999999/ },
    { participant2: /^temp_/ },
    { participant2: /^local_/ }
  ]
})
```

### После очистки

1. **Очистите localStorage в браузере:**
   - F12 → Application → Storage → Local Storage
   - Удалите всё или только `currentUser`

2. **Зарегистрируйтесь заново** через приложение

3. **Новый getTelegramId()** будет:
   - Использовать реальный Telegram ID (если в Telegram)
   - Сохранять ID при регистрации (если в браузере)
   - Использовать сохранённый ID при следующих входах

## Проверка

После регистрации проверьте в консоли:
```javascript
localStorage.getItem('currentUser')
```

Вы должны увидеть:
```json
{
  "id": "670170626",  // Ваш реальный ID из регистрации
  "telegramId": "670170626",
  "nickname": "ваш_ник"
}
```

Теперь при каждом входе будет использоваться этот же ID!
