# 🚀 ПОШАГОВАЯ ИНСТРУКЦИЯ ПО ДЕПЛОЮ НА RENDER.COM

## ✅ ШАГ 1: Регистрация на Render (2 минуты)

1. Открой https://render.com
2. Нажми **"Get Started for Free"**
3. Выбери **"Sign in with GitHub"**
4. Авторизуй Render доступ к своему GitHub

---

## 📦 ШАГ 2: Создание базы данных PostgreSQL (3 минуты)

1. На главной странице Render нажми **"New +"** → **"PostgreSQL"**

2. Заполни форму:
   ```
   Name: berloga-db
   Database: berloga
   User: berloga_user
   Region: Frankfurt (EU Central)
   Instance Type: Free
   ```

3. Нажми **"Create Database"**

4. **ВАЖНО!** После создания скопируй **"External Database URL"**
   - Он выглядит так: `postgresql://berloga_user:XXXX@dpg-XXXX.frankfurt-postgres.render.com/berloga`
   - Сохрани его в блокноте - понадобится на следующем шаге!

5. Подожди 2-3 минуты пока база создастся (статус станет "Available")

---

## 🖥️ ШАГ 3: Деплой Backend сервера (5 минут)

1. На Render нажми **"New +"** → **"Web Service"**

2. Нажми **"Build and deploy from a Git repository"** → **"Next"**

3. Найди и выбери свой репозиторий **"KupiyProday"** → **"Connect"**

4. Заполни настройки:
   ```
   Name: berloga-api
   Region: Frankfurt (EU Central)
   Branch: main
   Root Directory: backend
   Runtime: Node
   Build Command: npm install
   Start Command: npm start
   Instance Type: Free
   ```

5. **ОЧЕНЬ ВАЖНО!** Добавь переменные окружения (Environment Variables):
   
   Нажми **"Add Environment Variable"** и добавь ДВЕ переменные:
   
   **Переменная 1:**
   ```
   Key: DATABASE_URL
   Value: (вставь сюда External Database URL из Шага 2)
   ```
   
   **Переменная 2:**
   ```
   Key: NODE_ENV
   Value: production
   ```

6. Нажми **"Create Web Service"**

7. Подожди 5-7 минут пока пройдёт деплой
   - Увидишь логи сборки
   - В конце появится: "Your service is live 🎉"

8. **СКОПИРУЙ URL своего API!**
   - Он будет вверху страницы
   - Например: `https://berloga-api.onrender.com`

---

## 📱 ШАГ 4: Обновление Frontend (2 минуты)

1. Открой файл `frontend/.env`

2. Замени строку:
   ```env
   VITE_API_URL=http://192.168.0.235:3001
   ```
   
   На:
   ```env
   VITE_API_URL=https://berloga-api.onrender.com
   ```
   (используй СВОЙ URL из Шага 3, пункт 8)

3. Закоммить и запушить изменения:
   ```powershell
   cd C:\Users\User\Desktop\fgh\KupyProdai\frontend
   git add .
   git commit -m "Update API URL to Render cloud server"
   git push origin main
   ```

4. Netlify автоматически задеплоит новую версию (2-3 минуты)

---

## ✨ ШАГ 5: Проверка работы

1. Открой свой бот в Telegram

2. Зарегистрируй нового пользователя с другого телефона

3. Открой админ-панель на своём телефоне

4. **Новый пользователь появится через 3 секунды!** ✅

---

## 🎯 ГОТОВО!

Теперь:
- ✅ База данных в облаке (PostgreSQL на Render)
- ✅ Backend API в облаке (Node.js на Render)
- ✅ Frontend на Netlify
- ✅ Все пользователи видны всем
- ✅ Объявления синхронизируются
- ✅ Работает 24/7 без твоего компьютера!

---

## 🔧 Полезные ссылки после деплоя:

- **Backend Dashboard**: https://dashboard.render.com
- **База данных**: Там же, раздел PostgreSQL
- **Логи сервера**: В Render Dashboard → твой сервис → Logs
- **Health Check**: `https://berloga-api.onrender.com/health`

---

## ⚠️ ВАЖНО:

**Бесплатный план Render:**
- ✅ Безлимитные запросы
- ⚠️ Сервер "засыпает" после 15 минут неактивности
- ⚠️ Первый запрос после сна = 30-50 секунд загрузки
- ✅ Потом работает быстро

**Решение:** Первый запрос будет медленным, потом всё ОК!

---

Если что-то не получается - скажи на каком шаге проблема! 🐻
