# Процесс регистрации пользователя

## Шаг за шагом

### 1️⃣ Пользователь открывает бота (@Kupyiprodaybot)
- Нажимает кнопку **Menu** или команду **/start**
- Telegram открывает Mini App: `https://a8a3ca83.kupyprodai.pages.dev`

### 2️⃣ Получение Telegram ID
- Frontend получает данные из `window.Telegram.WebApp.initDataUnsafe.user`
- Извлекается:
  - **Telegram ID** (уникальный числовой ID пользователя)
  - **Telegram Username** (если установлен)
- Код: `frontend/src/utils/telegram.ts` → функция `getTelegramId()`

### 3️⃣ Проверка существующего пользователя
- App проверяет в базе данных: есть ли пользователь с этим Telegram ID?
- Запрос: `GET /users/telegram/:telegramId`
- Если **найден** → автоматический вход (autologin)
- Если **не найден** → показываем экран регистрации

### 4️⃣ Регистрация (4 шага)

#### Шаг 1: Выбор языка
- Страница: `WelcomePage.tsx`
- Пользователь выбирает язык интерфейса (RU, UA, PL, DE, EN, ES, FR)
- Сохраняется в: `localStorage` и `Zustand store`

#### Шаг 2: Выбор страны
- Страница: `CountryPage.tsx`
- Список стран с флагами
- Сохраняется в: `localStorage.registrationCountry`

#### Шаг 3: Выбор города и радиуса
- Страница: `CityPage.tsx` → `RadiusPage.tsx`
- Пользователь выбирает город из списка
- Устанавливает радиус поиска (5-100 км)
- Сохраняется в: `localStorage.registrationCity`, `localStorage.registrationRadius`

#### Шаг 4: Выбор никнейма
- Страница: `NicknamePage.tsx`
- **КРИТИЧНО:** Проверка уникальности никнейма

##### Проверка уникальности никнейма:

**На фронтенде (NicknamePage.tsx):**
```typescript
// Debounce проверка каждые 500мс
const checkResponse = await userAPI.checkNickname(nickname);
if (!checkResponse.data.available) {
  setError('Никнейм уже занят');
}
```

**На бэкенде (backend/src/routes/users.js):**
```javascript
// Эндпоинт: GET /users/check-nickname/:nickname
router.get('/check-nickname/:nickname', async (req, res) => {
  const user = await User.findOne({ nickname: req.params.nickname });
  res.json({ available: !user });
});

// При регистрации: POST /users/register
// Проверка для новых пользователей
if (!user) {
  const existingNickname = await User.findOne({ nickname });
  if (existingNickname) {
    return res.status(400).json({ message: 'Никнейм уже занят' });
  }
}

// Проверка при обновлении (если пользователь меняет никнейм)
if (user && user.nickname !== nickname) {
  const existingNickname = await User.findOne({ nickname });
  if (existingNickname && existingNickname.telegramId !== id) {
    return res.status(400).json({ message: 'Никнейм уже занят' });
  }
}
```

### 5️⃣ Отправка данных на сервер

**Данные отправляются на: `POST /users/register`**

```json
{
  "id": "670170626",              // Telegram ID
  "telegramId": "670170626",      // Дублируем для совместимости
  "nickname": "Adminisus",        // Выбранный никнейм (УНИКАЛЬНЫЙ!)
  "telegramUsername": "Danylo24", // Username из Telegram (если есть)
  "country": "DE",                // Код страны
  "city": "Вильгельмсхафен",      // Город
  "radius": 50,                   // Радиус поиска (км)
  "language": "ru",               // Язык интерфейса
  "contacts": {}                  // Контакты (пока пусто)
}
```

### 6️⃣ Сохранение в MongoDB Atlas

**Backend проверяет и сохраняет:**

```javascript
// 1. Проверка: существует ли пользователь с таким Telegram ID?
let user = await User.findOne({ telegramId: id });

if (user) {
  // Пользователь существует → обновляем данные
  user.nickname = nickname;
  user.city = city;
  // ... обновляем остальное
  await user.save();
} else {
  // 2. Проверка уникальности никнейма
  const existingNickname = await User.findOne({ nickname });
  if (existingNickname) {
    return res.status(400).json({ message: 'Никнейм уже занят' });
  }
  
  // 3. Создаём нового пользователя
  user = new User({
    telegramId: id,
    nickname,
    telegramUsername,
    country,
    city,
    radius,
    language,
    contacts: {}
  });
  await user.save();
}

// 4. Отправляем Socket.IO событие для обновления админ-панели в реальном времени
req.app.get('io').emit('user-registered', user);
```

### 7️⃣ Данные попадают к администратору

#### В админ-панели (AdminPage.tsx):
- Socket.IO слушает событие `'user-registered'`
- Новый пользователь мгновенно появляется в списке
- Доступ: открыть бот → нажать "Админ панель"
- URL: `https://a8a3ca83.kupyprodai.pages.dev/admin`

#### В текстовом файле (export-users.ps1):
```powershell
# Запуск скрипта на рабочем столе:
cd C:\Users\User\Desktop\fgh\KupyProdai
.\export-users.ps1

# Или напрямую в PowerShell:
$response = Invoke-WebRequest -Uri "https://kupiyproday.onrender.com/api/users" -UseBasicParsing
$users = $response.Content | ConvertFrom-Json
$users | Format-Table nickname, telegramId, city
```

**Файл сохраняется:** `Desktop\KupyProdai_Users_YYYY-MM-DD_HH-mm.txt`

### 8️⃣ Синхронизация Telegram ID ↔ Никнейм

**Telegram ID** — это **PRIMARY KEY** (основной ключ):
- Уникальный для каждого пользователя Telegram
- Никогда не меняется
- Используется для аутентификации

**Никнейм** — это **UNIQUE** (уникальное поле):
- Выбирается пользователем при регистрации
- **НЕ может совпадать** с никнеймом другого пользователя
- Используется для отображения в интерфейсе

**Связь:**
```javascript
// В базе MongoDB:
{
  _id: ObjectId("..."),                // MongoDB ID
  telegramId: "670170626",             // УНИКАЛЬНЫЙ! (PRIMARY KEY)
  nickname: "Adminisus",               // УНИКАЛЬНЫЙ! (проверяется)
  telegramUsername: "Danylo24",        // НЕ уникальный (может отсутствовать)
  city: "Вильгельмсхафен",
  createdAt: "2025-11-28T20:33:00Z"
}
```

## Проверки уникальности

### ✅ Telegram ID:
- Проверяется автоматически MongoDB (индекс `unique: true`)
- Если пользователь с таким ID существует → **обновление данных**
- Если не существует → **создание нового**

### ✅ Никнейм:
- Проверяется **дважды**:
  1. **На фронтенде** (debounce 500мс): визуальная индикация ✅/❌
  2. **На бэкенде** (при POST /register): финальная проверка перед сохранением

- Если занят → ошибка `400 Bad Request: "Никнейм уже занят"`

## API Эндпоинты

### 📍 GET `/users`
Получить всех пользователей (для админа)
```json
[
  {
    "_id": "...",
    "telegramId": "670170626",
    "nickname": "Adminisus",
    "city": "Вильгельмсхафен",
    "country": "DE",
    "radius": 50,
    "createdAt": "2025-11-28T20:33:00Z"
  }
]
```

### 📍 GET `/users/telegram/:telegramId`
Проверить существование пользователя по Telegram ID
- **200** → пользователь найден
- **404** → пользователь не найден

### 📍 GET `/users/check-nickname/:nickname`
Проверить доступность никнейма
```json
{ "available": true }  // или false
```

### 📍 POST `/users/register`
Регистрация/обновление пользователя
- Если `telegramId` существует → обновление
- Если не существует → создание
- Проверяет уникальность никнейма
- Возвращает объект пользователя

## Безопасность

### 🔐 Аутентификация через Telegram
- Frontend отправляет `initData` или `initDataUnsafe`
- Backend проверяет подпись через middleware `verifyTelegramAuth`
- Невозможно подделать Telegram ID

### 🔐 Проверка уникальности
- MongoDB индексы: `{ telegramId: 1, unique: true }`
- Бэкенд проверяет никнейм перед сохранением
- Двойная проверка (фронт + бэк) предотвращает дубликаты

## Мониторинг

### 📊 Просмотр пользователей:
1. **Админ-панель**: реальное время через Socket.IO
2. **Текстовый файл**: export-users.ps1 → Desktop
3. **MongoDB Atlas**: прямой доступ к базе данных
4. **Backend логи**: Render.com → Logs

### 🔔 Уведомления:
- Socket.IO события: `'user-registered'`, `'user-updated'`
- Админ-панель обновляется мгновенно
- Логи в консоли бэкенда

## Итого

✅ **Telegram ID** → уникальный идентификатор пользователя  
✅ **Никнейм** → уникальное имя (проверяется при регистрации)  
✅ **Данные → MongoDB Atlas** → Render.com backend  
✅ **Мониторинг → Админ-панель + текстовый файл**  
✅ **Синхронизация** в реальном времени через Socket.IO
